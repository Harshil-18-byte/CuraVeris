# Legacy Vite foundation

`src/main.tsx` and `vite.config.ts` are retained for compatibility with the original Phase 1 scaffold. The active web runtime is Next.js App Router under `src/app/`; these files are excluded from the Next TypeScript project and must not be imported by new application code.
